package com.developersphere.clock.domain.enum

enum class Day(val code: String) {
    Sunday(code = "S"),
    Monday(code = "M"),
    Tuesday(code = "T"),
    Wednesday(code = "W"),
    Thursday(code = "T"),
    Friday(code = "F"),
    Saturday(code = "S")
}